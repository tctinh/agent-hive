# Release v1.3.2

Compare: [`v1.3.1...v1.3.2`](https://github.com/tctinh/agent-hive/compare/v1.3.1...v1.3.2)

---

## Highlights

### Corrected Historical Note for PR #62

The published `v1.3.2` release did not yet include PR #62 / `488aa29` in its literal release-branch history, even though later reconstruction work proved that PR's tree matches the shipped `1.3.3` patch line. This corrected historical note keeps `v1.3.2` technically honest and explains that the formal PR #62 history alignment arrives in `v1.3.4`.

**Why it matters:** Users can trust that `v1.3.2` describes what was actually published at the time instead of back-projecting later PR #62 history alignment onto an earlier tag.

### Status Manifest Test Stabilization Also Shipped (`6a2d870`)

The published `v1.3.2` tag includes one additional follow-up fix after the original release notes draft: commit `6a2d870` stabilized the VS Code status manifest regression coverage in `packages/vscode-hive/src/tools/status.test.ts`.

**Why it matters:** This was part of the real published artifact even though the original notes were written one commit earlier. The historical record now reflects what users actually received in `v1.3.2`.

---

## What Changed

### Corrected: release history wording
- Removed the earlier implication that PR #62 or `488aa29` had already shipped in `v1.3.2`
- Recorded that the `1.3.x` release branch did not yet include PR #62 as literal branch history at this tag
- Pointed future readers to `v1.3.4` for the formal PR #62 history alignment note

### Preserved: shipped `v1.3.2` follow-up fix
- Kept the documented `6a2d870` status manifest stabilization that was part of the published `v1.3.2` tag
- Preserved the explanation that the original release notes draft predated that final shipped follow-up commit

### Tests and verification coverage
- Historical verification now checks that `v1.3.2` does not claim PR #62 shipped there and instead carries this corrected historical note
- Included the shipped `6a2d870` status manifest test stabilization that landed with the final published tag

---

## Breaking Changes

None. This corrected historical note does not change the published `v1.3.2` artifact; it only fixes the release narrative around what the tag actually contained.

---

## Upgrade Notes

- Upgrade targets the published `1.3.x` line directly: use `v1.3.2` as the shipped release tag for this branch.
- If you are auditing PR #62 history, note that `v1.3.2` did not yet include PR #62 as branch history; use `v1.3.4` for the formal PR #62 history alignment release note.

---

## Stats

- **Integrated sources called out explicitly**: corrected historical note, the not-yet-shipped-at-the-time PR #62 distinction, and the shipped `6a2d870` status manifest test stabilization
- **23 commits** between `v1.3.1` and this release branch state
- **57 files changed** since `v1.3.1` (`3,354` insertions, `512` deletions)
- **Published artifact note**: `v1.3.2` was subsequently released to npm and the VS Code Marketplace, but the formal PR #62 history alignment arrives in `v1.3.4`
